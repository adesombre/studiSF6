{% extends 'base.html.twig' %}

{% block title %}Hello SortieController!{% endblock %}

{% block body %}
  <h1>les sorties</h1>
<table class="table">
   
        <tr>
            <th scope="col">id</th>
            <td scope="col">{{ sortie.id }}</td>
        </tr>
        <tr>
            <th scope="col">tire </th>
            <td scope="row">{{ sortie.titre }}</td>
        </tr>
         <tr>
            <th scope="col">description </th>
            <td scope="row">{{ sortie.description }}</td>
        </tr>
         <tr>
            <th scope="col">lieu de sortie</th>
            <td scope="row">{{ sortie.lieudesortie}}</td>
        </tr>
         <tr>
            <th scope="col">date </th>
            <td scope="row">{{ sortie.datedesortie }}</td>
        </tr>
         <tr>
            <th scope="col">lieu de rdv </th>
            <td scope="row">{{ sortie.lieuduRDV }}</td>
        </tr>
</table>
<a href="{{ path('sortie_index') }}" class="btn btn-primary">aller a la liste</a>


{% endblock %}
