{% extends 'base.html.twig' %}

{% block title %}Modele{% endblock %}

{% block body %}
<table class="table">
    <thead>
        <tr>
            <th scope= "col">id</th>
            <th scope= "col">nom</th>
            <th scope= "col"></th>
        </tr>
    </thead>
    
<tbody>
        {% for modele in modeles %}
        <tr>
            <td scope= "row">{{ modele.id }}</td>
            <td scope= "row">{{ modele.libelle }}</td>
            <td scope= "row" >
                <a href="{{ path('modele_show',{'id':modele.id}) }}" class="btn btn-primary">details</a>
                <a href="#" class="btn btn-primary">modifier</a>
                <a href="#" class="btn btn-primary">supprimer</a> 
            </td>
        </tr>
    {% endfor %}
    </tbody>
    </table>
     <a href="#" class="btn btn-primary">ajouter un modele</a>
{% endblock %}
